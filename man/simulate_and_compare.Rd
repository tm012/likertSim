% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_likert.R
\name{simulate_and_compare}
\alias{simulate_and_compare}
\title{Simulate and Compare Likert Data}
\usage{
simulate_and_compare(
  df_real,
  K_min,
  K_max,
  N_sim = nrow(df_real),
  noise_factor = 1,
  bias = 0,
  corr_alpha = 0,
  seed_default = 123,
  seed_stress = 456,
  id_prefix_default = "sim",
  id_prefix_stress = "stress",
  mode = c("threshold", "meansd"),
  auto_small_switch = TRUE,
  small_n_cutoff = 15,
  threshold_strategy = c("empirical", "equal")
)
}
\arguments{
\item{df_real}{Data frame with first column \code{coder_id} and remaining columns Likert items.}

\item{K_min, K_max}{Integer bounds of the Likert scale.}

\item{N_sim}{Number of simulated raters (rows).}

\item{noise_factor}{Scale factor for variability (>1 = more spread).}

\item{bias}{Global shift (+ lenient, - severe).}

\item{corr_alpha}{Blend toward independence (0 = keep real R, 1 = diagonal).}

\item{mode}{Either \code{"threshold"} (ordinal) or \code{"meansd"}.}

\item{auto_small_switch}{Switch to mean–SD mode if N < cutoff.}

\item{small_n_cutoff}{Cutoff for auto switch.}

\item{threshold_strategy}{\code{"empirical"} (match real marginals) or \code{"equal"}.}
}
\value{
A list with components:
\describe{
\item{model}{Fitted model object (thresholds, correlations, means, sds).}
\item{sim_default}{Simulated dataset with no stress.}
\item{sim_stress}{Simulated dataset with stress knobs.}
\item{comparison}{Summary of means and SDs across datasets.}
\item{warnings}{Any saturation warnings.}
}
}
\description{
Fits a Likert simulation model from real data, generates simulated datasets
under baseline and stressed conditions, and compares means and standard deviations.
}
\examples{
df <- data.frame(coder_id=1:20, A=sample(1:5,20,TRUE), B=sample(1:5,20,TRUE))
out <- simulate_and_compare(df, K_min=1, K_max=5, N_sim=100)
head(out$comparison)

}
